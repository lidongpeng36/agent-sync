version = 1
# default_peer = "mini"

[peers.mini]
host = "mini"
ssh = "/usr/bin/ssh"
rsync = "rsync"

[agents.codex]
local_root = "~/.codex"

[agents.claude]
local_root = "~/.claude"
conflict_strategy = "merge"

[peers.mini.roots]
codex = ".codex"
claude = ".claude"
